<Window x:Class="DeckShare.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DeckShare" Width="1120" Height="820" MinWidth="920" MinHeight="680"
        FontFamily="Segoe UI" FontSize="13" WindowStartupLocation="CenterScreen">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="DeckShare" FontSize="24" FontWeight="SemiBold"/>
                <TextBlock Text="Windows → Steam Deck" Foreground="#666"/>
            </StackPanel>
            <ComboBox x:Name="LanguageBox" Grid.Column="1" Width="80" SelectionChanged="LanguageBox_SelectionChanged">
                <ComboBoxItem Content="RU" Tag="ru"/>
                <ComboBoxItem Content="EN" Tag="en"/>
            </ComboBox>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <GroupBox x:Name="ConnectionGroup" Grid.Column="0">
                <Grid Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock x:Name="HostLabel" Grid.Row="0" VerticalAlignment="Center"/>
                    <TextBox x:Name="HostBox" Grid.Row="0" Grid.Column="1" Margin="4"/>
                    <TextBlock x:Name="PortLabel" Grid.Row="1" VerticalAlignment="Center"/>
                    <TextBox x:Name="PortBox" Grid.Row="1" Grid.Column="1" Margin="4"/>
                    <TextBlock x:Name="UserLabel" Grid.Row="2" VerticalAlignment="Center"/>
                    <TextBox x:Name="UserBox" Grid.Row="2" Grid.Column="1" Margin="4"/>
                    <TextBlock x:Name="DefaultPathLabel" Grid.Row="3" VerticalAlignment="Center"/>
                    <TextBox x:Name="DefaultPathBox" Grid.Row="3" Grid.Column="1" Margin="4"/>
                    <TextBlock x:Name="AuthLabel" Grid.Row="4" VerticalAlignment="Center"/>
                    <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal" Margin="4">
                        <RadioButton x:Name="PasswordRadio" GroupName="Auth" Checked="AuthRadio_Checked" Margin="0,0,16,0"/>
                        <RadioButton x:Name="KeyRadio" GroupName="Auth" Checked="AuthRadio_Checked"/>
                    </StackPanel>
                    <TextBlock x:Name="SecretLabel" Grid.Row="5" VerticalAlignment="Center"/>
                    <StackPanel Grid.Row="5" Grid.Column="1">
                        <PasswordBox x:Name="SecretBox" Margin="4" MinHeight="26"/>
                        <CheckBox x:Name="SavePasswordCheck" Margin="4"/>
                    </StackPanel>
                    <TextBlock x:Name="IdentityLabel" Grid.Row="6" VerticalAlignment="Center"/>
                    <Grid Grid.Row="6" Grid.Column="1" Margin="4">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <TextBox x:Name="IdentityBox"/>
                        <Button x:Name="IdentityBrowseButton" Grid.Column="1" Margin="6,0,0,0" Click="IdentityBrowseButton_Click"/>
                    </Grid>
                    <TextBlock x:Name="ParallelLabel" Grid.Row="7" VerticalAlignment="Center"/>
                    <ComboBox x:Name="ParallelBox" Grid.Row="7" Grid.Column="1" Width="70" HorizontalAlignment="Left" Margin="4"/>
                </Grid>
            </GroupBox>

            <GroupBox x:Name="SourcesGroup" Grid.Column="2">
                <Grid Margin="10">
                    <Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                    <DataGrid x:Name="SourcesGrid" AutoGenerateColumns="False" CanUserAddRows="False" SelectionMode="Extended">
                        <DataGrid.Columns>
                            <DataGridTextColumn x:Name="LocalPathColumn" Binding="{Binding LocalPath}" Width="2*" IsReadOnly="True"/>
                            <DataGridTextColumn x:Name="RemotePathColumn" Binding="{Binding RemotePath, UpdateSourceTrigger=PropertyChanged}" Width="2*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                        <Button x:Name="AddButton" Click="AddButton_Click" Margin="0,0,8,0"/>
                        <Button x:Name="RemoveButton" Click="RemoveButton_Click"/>
                    </StackPanel>
                </Grid>
            </GroupBox>
        </Grid>

        <Grid Grid.Row="2" Margin="0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="TestButton" Click="TestButton_Click" Margin="0,0,8,0"/>
            <Button x:Name="TransferButton" Grid.Column="1" Click="TransferButton_Click" Margin="0,0,8,0"/>
            <Button x:Name="StopButton" Grid.Column="2" Click="StopButton_Click" IsEnabled="False"/>
            <TextBlock x:Name="StatusText" Grid.Column="4" VerticalAlignment="Center"/>
        </Grid>

        <GroupBox x:Name="ProgressGroup" Grid.Row="3">
            <Grid Margin="10">
                <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBlock x:Name="TotalEtaText" FontWeight="SemiBold"/>
                    <TextBlock Text="  ·  " Foreground="#777"/>
                    <TextBlock x:Name="TotalSpeedText" FontWeight="SemiBold"/>
                </StackPanel>
                <DataGrid x:Name="ProgressGrid" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn x:Name="FileColumn" Binding="{Binding FileName}" Width="2*"/>
                        <DataGridTextColumn x:Name="DestinationColumn" Binding="{Binding Destination}" Width="3*"/>
                        <DataGridTextColumn x:Name="SpeedColumn" Binding="{Binding Speed}" Width="110"/>
                        <DataGridTextColumn x:Name="PercentColumn" Binding="{Binding Percent}" Width="90"/>
                        <DataGridTextColumn x:Name="EtaColumn" Binding="{Binding Eta}" Width="90"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </GroupBox>

        <GridSplitter Grid.Row="4" Height="8" HorizontalAlignment="Stretch" Background="Transparent"/>

        <GroupBox x:Name="LogGroup" Grid.Row="5">
            <TextBox x:Name="LogBox" Margin="10" IsReadOnly="True" AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                     FontFamily="Consolas" TextWrapping="NoWrap"/>
        </GroupBox>
    </Grid>
</Window>
